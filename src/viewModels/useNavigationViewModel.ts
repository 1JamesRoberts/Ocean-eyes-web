// useNavigationViewModel.ts - Thin wrapper that re-exports useNavigation
// so Views do not import the NavigationContext directly.
export { useNavigation as useNavigationViewModel } from '../context/NavigationContext';
